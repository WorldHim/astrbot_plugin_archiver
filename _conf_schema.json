{
  "use_forward": {
    "description": "以聊天记录(合并转发)形式输出收录回执与语录",
    "hint": "不支持合并转发的平台(如部分微信/Telegram 接入)可关闭,回退为文本输出",
    "type": "bool",
    "default": true
  },
  "upload_permission": {
    "description": "上传(保存)权限",
    "hint": "群员=所有人；管理员=群管理员+群主+Bot管理员；群主=群主+Bot管理员；Bot管理员=仅Bot管理员。私聊消息无法查询群信息,按Bot管理员判定",
    "type": "string",
    "options": ["群员", "管理员", "群主", "Bot管理员"],
    "default": "群员"
  },
  "delete_permission": {
    "description": "删除权限",
    "hint": "群员=所有人；管理员=群管理员+群主+Bot管理员；群主=群主+Bot管理员；Bot管理员=仅Bot管理员。私聊消息无法查询群信息,按Bot管理员判定",
    "type": "string",
    "options": ["群员", "管理员", "群主", "Bot管理员"],
    "default": "管理员"
  },
  "owner_delete": {
    "description": "允许语录归属者删除自己的语录",
    "hint": "开启后,语录归属人(被收录语录的说话人,以QQ号匹配)即使不满足删除权限级别,也可删除归属为自己的语录",
    "type": "bool",
    "default": true
  },
  "session_quote_limit": {
    "description": "每个会话语录库的收录上限(条)",
    "hint": "超出后自动淘汰最早的语录;0 表示不限制",
    "type": "int",
    "default": 0
  },
  "fallback_global": {
    "description": "当前会话语录库为空时,是否从所有会话的语录库中随机抽取",
    "type": "bool",
    "default": false
  },
  "image_compress_threshold_mb": {
    "description": "收录图片超过该大小时自动压缩(MB)",
    "hint": "逐步重新编码并等比缩小直到达标;0 表示不压缩,始终保存原图",
    "type": "int",
    "default": 2
  }
}
