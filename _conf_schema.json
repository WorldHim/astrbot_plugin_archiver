{
  "session_quote_limit": {
    "description": "每个会话典库的收录上限(条)",
    "hint": "超出后自动淘汰最早的典;0 表示不限制",
    "type": "int",
    "default": 0
  },
  "fallback_global": {
    "description": "当前会话典库为空时,是否从所有会话的典库中随机抽取",
    "type": "bool",
    "default": false
  },
  "image_compress_threshold_mb": {
    "description": "收录图片超过该大小时自动压缩(MB)",
    "hint": "逐步重新编码并等比缩小直到达标;0 表示不压缩,始终保存原图",
    "type": "int",
    "default": 2
  }
}
